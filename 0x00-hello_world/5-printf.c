#include <stdio.h>

/**
 * Main - Entry point
 * Print output with printf function.
 * Return Always 0 (Success)
*/

int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,");
	return (0);
}
